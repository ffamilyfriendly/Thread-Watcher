- need to work on sqlite so it's compliant with interface
- need to add commands for managing bot settings

desperatly need a cleanup of the config command. It's a total mess and does not work.
add more error handling.
Maybe add some unit tests?
