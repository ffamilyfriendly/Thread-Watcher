# 🦆 Debug Duck

> This is the gold standard of organisational methods. This file contains stuff I'm doing or having issues with so that I can remember it later on.

## BEFORE LAUNCH

**Auth & Legal**

- [ ] create Privacy Policy & Terms of Service
- [ ] apply (and get) msg content & guild members priv
- [ ] switch mistral from trial -> paid to stop content being used for training

**Infra**

- [X] MySql adapter: write & test a MySql adapter for the database
- [ ] Migration Script: write a migration script for V2 DB -> V3 DB
  - Primary focus on settings & patterns to regex
- [x] Look over setting bucket lifecycle rules on attachments (180 days?)

**Bot**

- [x] Cleanup ticket modules: give proper descriptions, colours, and consistent names
- [x] Clean up expired tickets & ticket messages
- [x] ensure an AI summary is generated on ticket being resolved

**Web**

- [ ] Basic landing page
- [x] Make resolve ticket button prettier & disable when ticket resolved

**Cleanup/chore**

- [ ] Update system prompts to try to limit prompt injection
- [ ] add "prompts.md" file or something similar to ensure the system prompts are open source too
- [X] Update example config files & add comments
- [ ] Remove unneccesary logs
- [ ] Ensure permission gates on commands word
- [ ] Ensure permission gates & auth works on routes
- [ ] Update Readme.md
- [ ] Update docs
- [ ] Update listing sites (top.gg / discord discovery)

**BIG DAY**

- [ ] **DEPLOY** 🚀🚀🚀
- [ ] sit back end enjoy the ride
- [ ] fight all the fires we will have to fight

## 2026-03-28

### Update Audit Service

Right now our audit handling is horrible. We have to manually add the audit log after we've done something and also sent an IPC event (if it happened in the shard context).
We should re-write the audit service to instead subscribe to events from the other services and log them. This means we need to slightly rewrite every single service but should go decently fast enough. _Also:_ by using an event subscription we can use these events later on for the security service

### Fully implement pipeline

right now some pipeline modules do not have valid representation on the pipeline runner. Fix this

### Top.gg voting

we should reward servers by allowing server-wide monitors while the vote is active (12H). We should also reward the server with some AI credits.

### Premium CTA reworks

we should give some deets about premium on a dedicated dashboard page (/dashboard/\[GUILD*ID\]/premium). This is also a good place to show the vote information and give a unique link for that server (top.gg passes any present URL query params on the vote page, so we pass guild_id for tracking). We should also look over that premium gates are existent and working. I think the whole ticket / pipeline part is in order but unsure about global /batch and monitors. I \_think* we're good but will have to double check.

### (cleanup) /batch & /auto

these commands currently use some convoluted "CommandContext" that is probably prone to breaking. The idea was to solve us accidentally responding to already responded events but we wrote really good util functions for this in the ticket module that we should use instead. This is not _critical_ as the commands work as expected in general.

## 2026-03-12

### Get informed consent before ticket pipeline

> Added! [77ac43d](https://github.com/ffamilyfriendly/Thread-Watcher/commit/77ac43d)
> TOS & Privacy Policy consent text added to the bottom of the pipeline starter embed and also added to the "ticket created" embed with an extra emphasis if ticket has AI summaries enabled
> insert "you agree to terms (termslink) by opening a ticket"
> if panel has GPT summary enabled should we also let the user know this when the ticket is created? They already consented to terms by creating a ticket but an extra warning might be good?

### Create message listener

> Added! [77ac43d](https://github.com/ffamilyfriendly/Thread-Watcher/commit/77ac43d)

we need a listener that listens for messages in OPEN tickets and passes these to the ticket manager

#### How do we handle deleted messages?

do we continue to store them? Do we also delete them? Do we mark them as `is_deleted` and show them to server staff only?

### create file manager service

> Added! [77ac43d](https://github.com/ffamilyfriendly/Thread-Watcher/commit/77ac43d)

this is needed for any attachments that a message might have

#### Figure out what to store

> Partly adressed in [77ac43d](https://github.com/ffamilyfriendly/Thread-Watcher/commit/77ac43d)
> I think ideally we'd expand this. We might want to consider AI/ML scanning but thats for after a V3 deploy

if we enforce a file size limit for non-premium guilds do we simply not store any files? How will that contribute to the UX? Should we instead store a "placeholder" that details why the attachment was not saved (too large, and not premium) and provide a direct link to the attachment / message.

What content do we store? Storing videos and images can be relevant as a user might take a screenshot of something that is relevant or otherwise screen-cap it. We **SHOULD** prune possibly dangerous files, like executables. We are not providing malware to people. Maybe we should create a `redacted_reason` to the message type / table which can tie in with the above point as well to provide context as to why a file might be missing.

we should set up a way to generate pre-signed R2 links. We do not want to provide a direct link in case that leaks.

we should also make it possible to report images from the web view. I **DO NOT** want to store anything sketchy. When reported, should we just flag it as "is_reported" for me to manually check and remove (in case of CSAM which should be reported to feds) or just delete it straight up?

### figure out AI summary segmenter

We should try to group messages based on time to get "conversations". However, what do we do if the messages are sparse in timeframe? We should never create a summary for one single message. Maybe set a lower limit of messages in a summary (5?)

## 2026-03-11

### Limits of /view-note

> Fixed in [0b9c787](https://github.com/ffamilyfriendly/Thread-Watcher/commit/77ac43d)
> We can display 10 full length notes at the same time so we're showing the 10 latest notes.
> For paginated notes the user can use the web UI

### Define behaviour for "Claim Ticket" button

> Fixed in [0b9c787](https://github.com/ffamilyfriendly/Thread-Watcher/commit/77ac43d)
> Assigning to tickets works as expected but so far this is stale data that does nothing

Right now this does nothing. At least make it change the value in the DB and then we can figure out a proper usecase after that lol. Thinking we can ping the assigned user if there's been a new message in the OPEN ticket but no answer from the user in a certain time. Some shit like that lol.

### Make sure all pipeline modules are implemented

Right now we're lacking on (i think) the assign name module. We'll not add any more modules until after V3 drops.

### Add proper errors (at least to ticket modules)

> Fixed partly in [0b9c787](https://github.com/ffamilyfriendly/Thread-Watcher/commit/77ac43d)
> Our error management is still lacking which is bad UX but we'll leave it until features are done

We created this new EmbeddableError type for a reason. Use it!

### Fix the /ticket-panels/ list view

right now that part looks horrible on the website. Make sure to make it look pwetty and limit to one panel (free) and 20 lanels (paid). I'm thinking we stop allowing creation / editing of a panel if a guild unsubscribes but we otherwise keep their panels working.

### Create a /ticket/[ticket_id] page 🎉

I THINK this would be the last page we create before V3 can be deployed. Show possible ticket actions, messages, summaries, and so on. Make it look pwetty.

---

## 2026-03-10

### Contract

> Fixed in [5e34025](https://github.com/ffamilyfriendly/Thread-Watcher/commit/5e34025)

Create more [CONTRACTS](/packages/shared/tickets/contracts.ts) as well as implement them on both [ticket_outputs](/packages/shared/tickets/ticket_outputs.ts) as well as ensuring that Modules use these contracts when creating [ValueContainters](/bot/src/modules/ticket/_pipeline/ValueContainter.ts). The issue right now is we're defining one set of exported variables for a given module in ticket_outputs.ts but we're also having to count on us exporting the same set of variables with the same name on the module exports.

### Remove "Starting Embed" refs

> Fixed in [5e34025](https://github.com/ffamilyfriendly/Thread-Watcher/commit/5e34025)

Right now we store a "resolve_embed" on the [Panel](/packages/shared/schemas/ticket.ts#L281). But as we're working with a pipeline we resolve dynamically and have a module called "Open Ticket" that takes an embed which is what we want to use anyways. **Remove** this 'resolve_embed' from database schema, zod, and +page.svelte page.

### Export all values on ModalQuestion

> fixed in [5e34025](https://github.com/ffamilyfriendly/Thread-Watcher/commit/5e34025)

Right now we're only exporting values for Text Input and User Select on [the ModalQuestion](/bot/src/modules/ticket/_pipeline/modules/ModalQuestion.ts). We want to make sure we dynamically export any and all data for the label components we support.

Do this **after** CONTRACT is done as we should use contracts for this.

### Investigate weird button behaviour

> fixed in [67ed3fd](https://github.com/ffamilyfriendly/Thread-Watcher/commit/67ed3fd)

on [NarrowAnswer](/bot/src/modules/ticket/_pipeline/modules/IssueNarrower.ts) there are cases when the "Clarify" button fails to open a Modal resulting in "interaction failed". This does not seemingly happen with the "Skip" button so the error should live somewhere in the clarify button handler
