# 🦆 Debug Duck

> This is the gold standard of organisational methods. This file contains stuff I'm doing or having issues with so that I can remember it later on.

## 2026-03-11

### Limits of /view-note

Figure out what the limits are of the sections we're using here. Battle test 25 notes and see if it works.

### Define behaviour for "Claim Ticket" button

Right now this does nothing. At least make it change the value in the DB and then we can figure out a proper usecase after that lol. Thinking we can ping the assigned user if there's been a new message in the OPEN ticket but no answer from the user in a certain time. Some shit like that lol.

### Make sure all pipeline modules are implemented

Right now we're lacking on (i think) the assign name module. We'll not add any more modules until after V3 drops.

### Add proper errors (at least to ticket modules)

We created this new EmbeddableError type for a reason. Use it!

### Fix the /ticket-panels/ list view

right now that part looks horrible on the website. Make sure to make it look pwetty and limit to one panel (free) and 20 lanels (paid). I'm thinking we stop allowing creation / editing of a panel if a guild unsubscribes but we otherwise keep their panels working.

### Create a /ticket/[ticket_id] page 🎉

I THINK this would be the last page we create before V3 can be deployed. Show possible ticket actions, messages, summaries, and so on. Make it look pwetty.

---

## 2026-03-10

### Contract

> Fixed in 5e3402538ab8b7e16ad5ba9ca5880f8a3b7011b8

Create more [CONTRACTS](/packages/shared/tickets/contracts.ts) as well as implement them on both [ticket_outputs](/packages/shared/tickets/ticket_outputs.ts) as well as ensuring that Modules use these contracts when creating [ValueContainters](/bot/src/modules/ticket/_pipeline/ValueContainter.ts). The issue right now is we're defining one set of exported variables for a given module in ticket_outputs.ts but we're also having to count on us exporting the same set of variables with the same name on the module exports.

### Remove "Starting Embed" refs

> Fixed in 5e3402538ab8b7e16ad5ba9ca5880f8a3b7011b8

Right now we store a "resolve_embed" on the [Panel](/packages/shared/schemas/ticket.ts#L281). But as we're working with a pipeline we resolve dynamically and have a module called "Open Ticket" that takes an embed which is what we want to use anyways. **Remove** this 'resolve_embed' from database schema, zod, and +page.svelte page.

### Export all values on ModalQuestion

> fixed in 5e3402538ab8b7e16ad5ba9ca5880f8a3b7011b8

Right now we're only exporting values for Text Input and User Select on [the ModalQuestion](/bot/src/modules/ticket/_pipeline/modules/ModalQuestion.ts). We want to make sure we dynamically export any and all data for the label components we support.

Do this **after** CONTRACT is done as we should use contracts for this.

### Investigate weird button behaviour

> fixed in 67ed3fd5252393c1283d92d186756b76d2dad36c

on [NarrowAnswer](/bot/src/modules/ticket/_pipeline/modules/IssueNarrower.ts) there are cases when the "Clarify" button fails to open a Modal resulting in "interaction failed". This does not seemingly happen with the "Skip" button so the error should live somewhere in the clarify button handler
